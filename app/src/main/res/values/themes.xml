<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/colorBlack</item>
        <item name="android:windowLightNavigationBar" tools:ignore="NewApi">false</item>
    </style>

    <style name="Card" parent="Widget.Material3.CardView.Filled">
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">0dp</item>
        <item name="cardBackgroundColor">@color/colorBackgroundCard</item>
        <item name="contentPadding">16dp</item>
        <item name="strokeWidth">0dp</item>
        <item name="android:layout_marginStart">16dp</item>
        <item name="android:layout_marginEnd">16dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginBottom">16dp</item>
    </style>

    <style name="MaterialButton" parent="Widget.Material3.Button.UnelevatedButton">
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_marginStart">16dp</item>
        <item name="android:layout_marginEnd">16dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">16dp</item>
        <item name="iconGravity">textStart</item>
        <item name="iconTint">@color/color_state_list_enabled_disabled_text</item>
        <item name="android:textColor">@color/color_state_list_enabled_disabled_text</item>
        <item name="backgroundTint">@color/color_state_list_enabled_disabled_background</item>
    </style>

    <style name="Checkbox" parent="Widget.Material3.CompoundButton.CheckBox">
        <item name="buttonTint">@color/colorPrimary</item>
        <item name="buttonIconTint">@color/colorWhite</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">0dp</item>
        <item name="android:textColor">@color/colorText</item>
    </style>

    <style name="TextView">
        <item name="android:textColor">@color/colorText</item>
        <item name="android:drawableTint">@color/colorText</item>
        <item name="android:padding">16dp</item>
    </style>

    <style name="OptionsHeadline">
        <item name="android:textColor">@color/colorText</item>
        <item name="android:textSize">18sp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:paddingTop">16dp</item>
        <item name="android:paddingBottom">0dp</item>
    </style>

    <style name="OptionsItem">
        <item name="android:textColor">@color/colorText</item>
        <item name="android:paddingStart">48dp</item>
        <item name="android:paddingEnd">8dp</item>
    </style>

    <style name="BottomSheetDialog" parent="ThemeOverlay.Material3.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/bottomSheetStyleWrapper</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowSoftInputMode">adjustResize|stateHidden</item>
    </style>

    <style name="bottomSheetStyleWrapper" parent="Widget.Material3.BottomSheet.Modal">
        <item name="backgroundTint">@color/colorBackground</item>
    </style>

    <style name="BottomSheetDialog.DragHandleView" parent="Widget.Material3.BottomSheet.DragHandle">
        <item name="tint">@color/colorStroke</item>
    </style>

    <style name="RippleColor" parent="@style/ThemeOverlay.AppCompat.Light">
        <item name="colorControlHighlight">@color/colorRipple</item>
    </style>

</resources>